// import React from 'react';

// export default function ChatButton({ onClick, isOpen }) {
//   return (
//     <button
//       onClick={onClick}
//       className={`fixed bottom-6 right-6 z-40 w-14 h-14 bg-blue-500 hover:bg-blue-600 text-white rounded-full shadow-lg transition-transform ${
//         isOpen ? 'transform scale-125' : ''
//       }`}
//       aria-label={isOpen ? 'Close chat' : 'Open chat'}
//     >
//       {isOpen ? '✕' : '💬'}
//     </button>
//   );
// }
